Can you take a look at @tasks/{test_id}/task.json task file and also use the data
in @tasks/{test_id}/data/ to produce a response within a folder name with
the template "tasks/{test_id}/answer/{model_name_here}"? Make sure to only access
these files and create the indicated folder. Do not use any code / history from
previous conversations. Replace "{model_name_here}" with the model that is
currently being used. Double check that the model name is the one used.
