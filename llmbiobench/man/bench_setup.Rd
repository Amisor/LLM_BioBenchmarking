% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{bench_setup}
\alias{bench_setup}
\title{Set up a complete single benchmark test}
\usage{
bench_setup(
  test_id,
  output_dir = getwd(),
  taskfile = "task.json",
  evalfile = "eval.json",
  timeout = 3600
)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test. Must not contain
whitespace.}

\item{output_dir}{\code{character(1)} path to the parent directory in which the
test folder should be created. Defaults to the current working
directory.}

\item{taskfile}{\code{character(1)} name of the task JSON file to write.
Defaults to \code{"task.json"}.}

\item{evalfile}{\code{character(1)} name of the eval JSON file to write.
Defaults to \code{"eval.json"}.}

\item{timeout}{\code{numeric(1)} timeout, in seconds, to record for the task.
Defaults to \code{3600}.}
}
\value{
\code{list} with elements \code{task} and \code{eval}, the objects written to
\code{taskfile} and \code{evalfile} respectively.
}
\description{
Convenience wrapper that runs the full setup for a single benchmarking
test: creates the folder structure with \code{\link[=bench_folders]{bench_folders()}}, then prompts
the user once for the task question and expected output description, and
reuses those same responses to write both \code{task.json} (via \code{\link[=bench_task]{bench_task()}})
and \code{eval.json} (via \code{\link[=bench_eval]{bench_eval()}}).
}
\examples{
\dontrun{
bench_setup("bioc-1-a", output_dir = tempdir())
}

}
