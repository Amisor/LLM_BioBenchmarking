% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{add_prompts}
\alias{add_prompts}
\title{Copy prompt templates into a benchmark test's prompt folder}
\usage{
add_prompts(test_id, output_dir = getwd(), version = "", overwrite = TRUE)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test. The corresponding
test directory (created by \code{\link[=bench_folders]{bench_folders()}}) must already exist.}

\item{output_dir}{\code{character(1)} path to the parent directory containing
the test folder. Defaults to the current working directory.}

\item{version}{\code{character(1)} which version of the prompts to copy.
Either \code{""} for the original prompts, or \code{"_v2"} for the second
version. Defaults to \code{""}.}

\item{overwrite}{\code{logical(1)} whether to overwrite existing files in the
destination \code{prompt} folder. Defaults to \code{TRUE}.}
}
\value{
\code{character()} the destination file paths that were written,
returned invisibly.
}
\description{
Copies the prompt template files bundled with the package (under
\code{inst/extdata/prompt}) into the \code{prompt} subfolder of a benchmark test,
substituting any \code{"{test_id}"} placeholder in the template text for the
actual \code{test_id} (the templates otherwise still reference the specific
task, e.g. \code{"bioc-1-a"}, they were first authored for). Two versions of
each prompt are shipped with the package: the original version (e.g.
\code{evalprompt.txt}) and a second version suffixed with \code{"_v2"} (e.g.
\code{evalprompt_v2.txt}). Use \code{version} to select which set of prompts to
copy.
}
\examples{
\dontrun{
bench_folders("bioc-1-a", output_dir = tempdir())
add_prompts("bioc-1-a", output_dir = tempdir(), version = "_v2")
}

}
