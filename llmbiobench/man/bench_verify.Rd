% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify.R
\name{bench_verify}
\alias{bench_verify}
\title{Independently verify one benchmark test's answer submissions}
\usage{
bench_verify(test_dir)
}
\arguments{
\item{test_dir}{\code{character(1)} path to a single benchmark test directory
(e.g. \code{"tasks/bioc-1-a"}), containing \code{eval.json}, \verb{ref_answer/}, and
\verb{answer/<model>/} submissions.}
}
\value{
\code{list} with elements \code{numeric} (a \code{data.frame} of per-file,
per-model numeric verdicts) and \code{rubric} (a named \code{list} of
per-model \code{data.frame}s of rubric criterion checks, \code{NULL} if no
\code{eval_v2.json} is present). Both are also printed for convenience.
}
\description{
Recomputes ground truth for a test's expected output(s) against every
model's submission in its \verb{answer/} folder: strict numeric-matrix
comparison for csv/tsv where the reference parses fully numeric, a
relaxed mixed string/numeric table comparison otherwise, sequence-set
comparison for fasta, scalar comparison for txt, and an explicit
\code{NEEDS_JUDGE} verdict (rather than a silent skip) for any other
extension with no mechanical comparator (png, vcf, bam, ...). If an
\code{eval_v2.json} rubric is present, mechanically pre-checks whichever
rubric criteria don't require an LLM judge (see
\code{\link[=mechanical_rubric_criteria]{mechanical_rubric_criteria()}}), reporting which fraction of the rubric's
weight is judge-free.
}
\details{
This is the same, already-tested logic as the standalone
\code{code/verify_task_judge.R} script, exposed as a package function
operating on a single test directory, so it can run immediately after
\code{\link[=bench_rubric]{bench_rubric()}}/\code{\link[=bench_task]{bench_task()}} rather than only as a separate,
after-the-fact script over the whole \verb{tasks/} tree.
}
