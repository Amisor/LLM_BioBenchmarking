% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_llm.R
\name{bench_llm}
\alias{bench_llm}
\title{Run a single LLM against one benchmark test via ellmer}
\usage{
bench_llm(
  test_id,
  provider = names(.ellmer_providers),
  model,
  output_dir = getwd(),
  prompt_version = c("", "_v2"),
  prompt_file = NULL,
  system_prompt = NULL,
  tools = NULL,
  echo = "none",
  max_turns = 20,
  ...
)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test. The corresponding
test directory (created by \code{\link[=bench_folders]{bench_folders()}}) must already exist and
contain a \code{task.json} (from \code{\link[=bench_task]{bench_task()}}) and, if \code{prompt_version} is
used, a copied prompt file (from \code{\link[=add_prompts]{add_prompts()}}).}

\item{provider}{\code{character(1)} which ellmer chat backend to use. One of
'openai', 'anthropic', 'google', 'azure', 'bedrock', 'ollama', 'databricks', 'groq', 'perplexity', 'deepseek', matching
the constructor names in ellmer's \verb{chat_*()} family.}

\item{model}{\code{character(1)} the model name to pass to the chosen
provider's \verb{chat_*()} constructor (e.g. \code{"gpt-4o"},
\code{"claude-opus-4-5"}, \code{"gemini-2.5-pro"}).}

\item{output_dir}{\code{character(1)} path to the parent directory containing
the test folder. Defaults to the current working directory.}

\item{prompt_version}{\code{character(1)} which prompt template to send,
matching \code{\link[=add_prompts]{add_prompts()}}'s \code{version}: \code{""} for the original prompt or
\code{"_v2"} for the second version. Defaults to \code{""}.}

\item{prompt_file}{\code{character(1)} optional explicit prompt file name
(relative to the test's \verb{prompt/} folder) to use instead of deriving
one from \code{prompt_version}. Defaults to \code{NULL}.}

\item{system_prompt}{\code{character(1)} optional system prompt passed to the
chat constructor's \code{system_prompt} argument. Defaults to \code{NULL}.}

\item{tools}{\code{list} of \code{ellmer::tool()} objects to register with the
chat. Defaults to \code{NULL}, which uses \code{\link[=bench_llm_tools]{bench_llm_tools()}} scoped to the
test directory.}

\item{echo}{\code{character(1)} passed through to the ellmer chat
constructor's \code{echo} argument, controlling whether the conversation is
streamed to the console. Defaults to \code{"none"}.}

\item{max_turns}{\code{numeric(1)} maximum number of assistant/tool turns to
allow before stopping, passed to \code{chat$set_turns()}-style limits if
supported by the installed ellmer version; otherwise advisory only.
Defaults to \code{20}.}

\item{...}{additional named arguments forwarded to the provider's
\verb{chat_*()} constructor (e.g. \code{api_args}, \code{base_url}, \code{api_key}).}
}
\value{
\code{list} with elements \code{model_name} (the identifier used for the
\verb{answer/<model_name>} folder), \code{response} (the final assistant text),
and \code{chat} (the \code{ellmer::Chat} object, for inspecting the full
transcript with e.g. \code{chat$get_turns()}). Returned invisibly.
}
\description{
Backbone function for driving one benchmarking call at a time to an LLM
provider supported by \href{https://ellmer.tidyverse.org/}{ellmer}, while
allowing the caller to choose from several providers/models across
invocations. Loads the test's prompt template (as copied by
\code{\link[=add_prompts]{add_prompts()}}), substitutes in the test id, equips the model with a
restricted file toolset scoped to the test directory (see
\code{\link[=bench_llm_tools]{bench_llm_tools()}}) so it can read \code{task.json}/\verb{data/} and write its
answer under \verb{answer/<model_name>}, and runs the chat to completion.
}
\details{
Only one provider/model pair is run per call; to benchmark several
providers or models, call this function once per combination (e.g. in a
loop over a small data frame of \code{provider}/\code{model} pairs).
}
\examples{
\dontrun{
bench_folders(test_id = "bioc-2-a", output_dir = "inst/benchmarks/")
bench_task(
    test_id = "bioc-2-a",
    output_dir = "inst/benchmarks/",
    question = "Using the provided RNA-seq gene-level SummarizedExperiment dataset (gse.rds), construct a DESeqDataSet object with design formula '~ cell + dex' (renaming donor to cell, condition to dex, setting levels 'untrt' and 'trt' with 'untrt' as reference). Pre-filter to retain genes with counts >= 10 in at least 4 samples, apply variance stabilizing transformation (vst) with blind=FALSE, and compute the Euclidean sample-to-sample distance matrix. Save the distance matrix with row names formatted as '<dex> - <cell>' and column names set to NULL.",
    output_file = "sample_dist_matrix.csv",
    output_description = "Euclidean sample distance matrix (8x8) computed from VST-transformed counts with rownames formatted as '<dex> - <cell>' (e.g. 'untrt - N61311') and without column names."
)
add_prompts("bioc-2-a", version = "_v2", output_dir = "inst/benchmarks/")
## run script to generate gse.rds
## source("inst/scripts/bioc-2-a.R")
add_data(gse, "bioc-2-a", type = "rds", output_dir = "inst/benchmarks/")
## copy ref_script into the test folder for reference
## file.copy(
##     "inst/scripts/sample_dist_matrix.R",
##     "inst/benchmarks/bioc-2-a/ref_script/sample_dist_matrix.R"
## )
## ## add reference answer for comparison
## file.copy(
##     "inst/scripts/sample_dist_matrix.csv",
##     "inst/benchmarks/bioc-2-a/ref_answer/sample_dist_matrix.csv"
## )

bench_llm(
    "bioc-2-a",
    provider = "anthropic",
    model = "claude-opus-4-5",
    output_dir = "inst/benchmarks/"
)
}

}
