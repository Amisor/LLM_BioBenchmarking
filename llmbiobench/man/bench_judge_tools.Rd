% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_llm.R
\name{bench_judge_tools}
\alias{bench_judge_tools}
\title{Build a judge-scoped toolset for grading one candidate's submission}
\usage{
bench_judge_tools(
  test_dir,
  candidate_model,
  judge_output_file = "judge_result.json"
)
}
\arguments{
\item{test_dir}{\code{character(1)} path to a single benchmark test directory.}

\item{candidate_model}{\code{character(1)} name of the \verb{answer/} subfolder
being graded.}

\item{judge_output_file}{\code{character(1)} file name (within
\verb{answer/<candidate_model>/}) the judge is permitted to write its
verdict to. Defaults to \code{"judge_result.json"}.}
}
\value{
\code{list} of \code{ellmer::tool()} objects: \code{read_file}, \code{write_file},
\code{list_files}.
}
\description{
The inverse of \code{\link[=bench_llm_tools]{bench_llm_tools()}}: an answering model must not see the
reference materials, but a judge model must. Grants read access to the
whole test directory (\code{task.json}, \verb{data/}, \verb{ref_answer/}, \verb{ref_script/},
\code{eval.json}, \code{eval_v2.json}) plus the one candidate's own
\verb{answer/<candidate_model>/} submission, but hides every \emph{other} model's
\verb{answer/} subfolder (from both \code{read_file} and \code{list_files}) so grading
one candidate can't be contaminated by seeing another candidate's
answer. Write access is restricted to a single file,
\verb{answer/<candidate_model>/<judge_output_file>}, so the judge cannot
modify the candidate's submitted output itself.
}
\keyword{internal}
