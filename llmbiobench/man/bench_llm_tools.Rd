% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_llm.R
\name{bench_llm_tools}
\alias{bench_llm_tools}
\title{Build a restricted file-access toolset scoped to one test directory}
\usage{
bench_llm_tools(test_dir, allow_write_prefix = "answer")
}
\arguments{
\item{test_dir}{\code{character(1)} path to a single benchmark test directory.}

\item{allow_write_prefix}{\code{character(1)} a path prefix (relative to
\code{test_dir}) that write/list operations are restricted to, in addition
to read access across the whole test directory. Defaults to \code{"answer"}.}
}
\value{
\code{list} of \code{ellmer::tool()} objects: \code{read_file}, \code{write_file},
\code{list_files}.
}
\description{
Constructs \code{\link[ellmer:tool]{ellmer::tool()}} definitions for reading, writing, and listing
files, each of which resolves paths relative to \code{test_dir} and refuses to
operate outside of it. This lets a model read \code{task.json}/\verb{data/} and
write its response under \verb{answer/<model_name>} without being able to
touch the rest of the filesystem (in particular, \verb{ref_answer/},
\verb{ref_script/}, and \code{eval*.json}, which would leak the reference answer).
}
\keyword{internal}
