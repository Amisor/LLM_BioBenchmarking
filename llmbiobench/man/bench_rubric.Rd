% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rubric.R
\name{bench_rubric}
\alias{bench_rubric}
\title{Create an \code{eval_v2.json} rubric-scored eval file for a benchmark test}
\usage{
bench_rubric(
  test_id,
  output_dir = getwd(),
  evalfile = "eval_v2.json",
  question = NULL,
  guidelines = NULL,
  has_row_names = FALSE,
  custom_criteria = list()
)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test. The corresponding
test directory (created by \code{\link[=bench_folders]{bench_folders()}}) must already exist, and
should contain populated \code{ref_answer} and \code{ref_script} subfolders.}

\item{output_dir}{\code{character(1)} path to the parent directory containing
the test folder. Defaults to the current working directory.}

\item{evalfile}{\code{character(1)} name of the JSON file to write within the
test directory. Defaults to \code{"eval_v2.json"}.}

\item{question}{\code{character(1)} optional task description. If \code{NULL}
(the default), the user is prompted interactively.}

\item{guidelines}{\code{character(1)} optional scoring guidelines applied to
each expected output file that the user names interactively. If \code{NULL}
(the default), the user is prompted for guidelines separately for each
output file.}

\item{has_row_names}{\code{logical(1)} passed to \code{\link[=mechanical_rubric_criteria]{mechanical_rubric_criteria()}}.}

\item{custom_criteria}{\code{list} of additional, task-specific rubric
criterion objects (each with \code{id}, \code{criterion}, \code{weight}, \code{score_type},
\code{evidence}) that require an LLM judge to evaluate, e.g. whether the
submitted script used the correct statistical method. Defaults to
\code{list()} (no task-specific criteria beyond the mechanical ones).}
}
\value{
\code{list} the eval object that was written to \code{evalfile}, returned
invisibly.
}
\description{
Extends \code{\link[=bench_eval]{bench_eval()}}'s simple \code{eval.json} with a weighted rubric: the
mechanically-checkable criteria from \code{\link[=mechanical_rubric_criteria]{mechanical_rubric_criteria()}} (no
judge needed for those, see \code{\link[=bench_verify]{bench_verify()}}) plus any task-specific
criteria supplied by the caller, which genuinely require an LLM judge to
read and evaluate the submitted code (e.g. "uses the correct statistical
method", "avoids unnecessary steps").
}
\examples{
\dontrun{
bench_folders("bioc-1-a", output_dir = tempdir())
bench_rubric(
    "bioc-1-a",
    output_dir = tempdir(),
    question = "Compute the mean of each column.",
    guidelines = "Values must match the reference within 1e-3.",
    custom_criteria = list(list(
        id = "uses_correct_input_file",
        criterion = "The submitted script reads the provided input file.",
        weight = 3, score_type = "binary",
        evidence = "Inspect the submitted script."
    ))
)
}

}
