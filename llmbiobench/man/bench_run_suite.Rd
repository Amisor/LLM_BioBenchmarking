% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_llm.R
\name{bench_run_suite}
\alias{bench_run_suite}
\title{Run a benchmark suite: multiple tasks x multiple models, judged and verified}
\usage{
bench_run_suite(
  test_ids,
  models,
  judge_provider,
  judge_model,
  output_dir = getwd(),
  prompt_version = c("", "_v2"),
  results_file = "leaderboard.csv",
  ...
)
}
\arguments{
\item{test_ids}{\code{character()} identifiers of the tests to run, matching
subdirectories of \code{output_dir}.}

\item{models}{\code{data.frame} with columns \code{provider} and \code{model}, one row
per provider/model pair to benchmark.}

\item{judge_provider, judge_model}{\code{character(1)} the provider/model to
use for grading via \code{\link[=bench_judge]{bench_judge()}}.}

\item{output_dir}{\code{character(1)} as in \code{\link[=bench_llm]{bench_llm()}}.}

\item{prompt_version}{\code{character(1)} as in \code{\link[=bench_llm]{bench_llm()}}/\code{\link[=bench_judge]{bench_judge()}}.}

\item{results_file}{\code{character(1)} file name (within \code{output_dir}) the
aggregated results table is written to as CSV. Defaults to
\code{"leaderboard.csv"}.}

\item{...}{additional arguments forwarded to \code{\link[=bench_llm]{bench_llm()}}.}
}
\value{
\code{data.frame} of results (also written to \code{results_file}), with
columns \code{test_id}, \code{model}, \code{source} (\code{"answer"}, \code{"judge"}, or
\code{"verify"}), \code{score}, \code{verdict}, \code{elapsed_seconds}, \code{error}. Also
printed for convenience.
}
\description{
Orchestrates the full answer/judge/verify loop across every combination
of \code{test_ids} and \code{models}: for each pair, calls \code{\link[=bench_llm]{bench_llm()}} to
produce an answer, \code{\link[=bench_judge]{bench_judge()}} to grade it, and (when
\code{eval_v2.json} is present) \code{\link[=bench_verify]{bench_verify()}} to mechanically pre-check the
rubric. Each step is wrapped in \code{tryCatch()} so a single failing
task/model combination is recorded as an error row rather than aborting
the whole run (matching \code{code/biomni_query.py}'s per-task
try/except pattern), and all results are aggregated into one long-format
results table.
}
\details{
Token/cost accounting is intentionally out of scope for this function;
only wall-clock elapsed time per step is captured.
}
