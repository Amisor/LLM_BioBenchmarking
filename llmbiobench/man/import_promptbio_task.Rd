% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_promptbio.R
\name{import_promptbio_task}
\alias{import_promptbio_task}
\title{Import a PromptBio-Bench task into the local \verb{tasks/} benchmark tree}
\usage{
import_promptbio_task(
  task_id,
  source_dir = "data/promptbio-bench/tasks",
  output_dir = "tasks",
  prompt_version = "_v2"
)
}
\arguments{
\item{task_id}{\code{character(1)} PromptBio-Bench task identifier, e.g.
\code{"a-1-1"}.}

\item{source_dir}{\code{character(1)} path to the local PromptBio-Bench
mirror (as populated by \code{download_promptbio_task_files()} in
\code{code/read_promptbio_huggingface.R}), containing
\verb{<source_dir>/<task_id>/\{task.json,eval.json,data/,ref_answer/,ref_script/\}}.
Defaults to \code{"data/promptbio-bench/tasks"}.}

\item{output_dir}{\code{character(1)} path to the parent directory in which
the task folder should be created, matching \code{\link[=bench_folders]{bench_folders()}}'s
\code{output_dir}. Defaults to \code{"tasks"}.}

\item{prompt_version}{\code{character(1)} which prompt template set to copy
in via \code{\link[=add_prompts]{add_prompts()}}, either \code{""} or \code{"_v2"}. Defaults to \code{"_v2"}.}
}
\value{
\code{character(1)} the \code{task_id}, invisibly returned.
}
\description{
Materializes a task already fetched from Hugging Face (via
\code{code/read_promptbio_huggingface.R}'s \code{download_promptbio_task_files()})
into the standard \code{\link[=bench_folders]{bench_folders()}} layout under \code{output_dir}, so it can
be run through \code{\link[=bench_llm]{bench_llm()}}/\code{\link[=bench_verify]{bench_verify()}} like any hand-authored
task. PromptBio-Bench's \code{task.json}/\code{eval.json} field names already match
\code{\link[=bench_task]{bench_task()}}/\code{\link[=bench_eval]{bench_eval()}}'s schema exactly (\code{id}, \code{question},
\code{input_files}, \code{expected_output}, \code{timeout_seconds} for \code{task.json};
\code{id}, \code{question}, \code{ref_answer}, \code{ref_script}, \code{scoring.expected_output}
for \code{eval.json}), so this is a straight materializer, folder creation
plus file copy, not a schema translation.
}
\examples{
\dontrun{
import_promptbio_task("a-1-1")
}

}
