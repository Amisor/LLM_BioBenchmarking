% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bench_llm.R
\name{bench_judge}
\alias{bench_judge}
\title{Run a single LLM judge against one benchmark test's candidate answer}
\usage{
bench_judge(
  test_id,
  provider = names(.ellmer_providers),
  model,
  candidate_model = NULL,
  output_dir = getwd(),
  prompt_version = c("", "_v2"),
  prompt_file = NULL,
  judge_output_file = "judge_result.json",
  system_prompt = NULL,
  echo = "none",
  max_turns = 20,
  ...
)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test.}

\item{provider}{\code{character(1)} which ellmer chat backend the judge uses;
see \code{\link[=bench_llm]{bench_llm()}}.}

\item{model}{\code{character(1)} the judge model name.}

\item{candidate_model}{\code{character(1)} the model whose \verb{answer/<model>/}
submission should be graded. If \code{NULL} (the default), every subfolder
of \verb{answer/} is graded in turn.}

\item{output_dir}{\code{character(1)} as in \code{\link[=bench_llm]{bench_llm()}}.}

\item{prompt_version}{\code{character(1)} which judge prompt template to send,
\code{""} for \code{judgeprompt.txt} or \code{"_v2"} for \code{judgeprompt_v2.txt}.
Defaults to \code{""}.}

\item{prompt_file}{\code{character(1)} optional explicit prompt file name,
overriding \code{prompt_version}. Defaults to \code{NULL}.}

\item{judge_output_file}{\code{character(1)} file name (within
\verb{answer/<candidate_model>/}) the judge is instructed to write its
verdict to. Defaults to \code{"judge_result.json"}.}

\item{system_prompt, echo, max_turns}{as in \code{\link[=bench_llm]{bench_llm()}}.}

\item{...}{additional named arguments forwarded to the provider's
\verb{chat_*()} constructor.}
}
\value{
If \code{candidate_model} is supplied, a \code{list} with elements
\code{model_name} (the judge model), \code{candidate_model}, \code{response}, and
\code{chat}. If \code{candidate_model} is \code{NULL}, a named \code{list} of such results,
one per \verb{answer/} subfolder. Returned invisibly.
}
\description{
Automates the grading step that, until now, has been done by hand (see
the \code{bioc-1-a} calibration data point in \code{documents/ideas.md}): loads the
test's judge prompt template (as copied by \code{\link[=add_prompts]{add_prompts()}}), equips the
model with \code{\link[=bench_judge_tools]{bench_judge_tools()}} (the inverse of \code{\link[=bench_llm_tools]{bench_llm_tools()}}:
allowed to read the reference materials, restricted to writing only the
verdict file), and instructs it to write a structured verdict to
\verb{answer/<candidate_model>/judge_result.json}. The verdict fields
(\code{score}, \code{verdict}, \code{confidence}, \code{rationale}) match the columns already
used in \code{data/judge_calibration/pilot/judge_scores.csv}, for continuity
with the existing calibration data.
}
\examples{
\dontrun{
bench_judge(
    "bioc-1-a",
    provider = "anthropic",
    model = "claude-opus-4-5",
    candidate_model = "gpt-5"
)
}

}
