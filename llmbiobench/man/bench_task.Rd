% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{bench_task}
\alias{bench_task}
\title{Create a \code{task.json} file for a benchmark test}
\usage{
bench_task(
  test_id,
  output_dir = getwd(),
  taskfile = "task.json",
  timeout = 3600,
  question = NULL,
  output_file = NULL,
  output_description = NULL
)
}
\arguments{
\item{test_id}{\code{character(1)} identifier for the test. The corresponding
test directory (created by \code{\link[=bench_folders]{bench_folders()}}) must already exist.}

\item{output_dir}{\code{character(1)} path to the parent directory containing
the test folder. Defaults to the current working directory.}

\item{taskfile}{\code{character(1)} name of the JSON file to write within the
test directory. Defaults to \code{"task.json"}.}

\item{timeout}{\code{numeric(1)} timeout, in seconds, to record for the task.
Defaults to \code{3600}.}

\item{question}{\code{character(1)} optional task description. If \code{NULL}
(the default), the user is prompted interactively.}

\item{output_file}{\code{character(1)} optional name of the expected output
file (e.g. \code{"sample_dist_matrix.csv"}). If \code{NULL} (the default), the
user is prompted interactively. Its extension is recorded as the
output \code{type}.}

\item{output_description}{\code{character(1)} optional description of the
expected task output. If \code{NULL} (the default), the user is prompted
interactively.}
}
\value{
\code{list} the task object that was written to \code{taskfile}, returned
invisibly.
}
\description{
Builds and writes a \code{task.json} file describing a benchmark task,
including the task question, the list of input data files, and a
description of the expected output. Prompts the user interactively for
the question, the expected output file name, and the output description
unless supplied directly.
}
\examples{
\dontrun{
bench_folders("bioc-1-a", output_dir = tempdir())
bench_task(
    "bioc-1-a",
    output_dir = tempdir(),
    question = "Compute the mean of each column.",
    output_file = "column_means.csv",
    output_description = "A CSV file with one row per column mean."
)
}

}
